function ResourceCard({ resource }) { return <article className="resource-card"><div className={`resource-icon ${resource.accent}`} aria-hidden="true">▤</div><div className="resource-copy"><span className="resource-type">{resource.type} · {resource.subject}</span><h3>{resource.title}</h3><div className="progress-label"><span>{resource.progress === 100 ? 'Complete' : `${resource.progress}% complete`}</span><span>{resource.progress}%</span></div><div className="progress-track"><span style={{ width: `${resource.progress}%` }} /></div></div></article> }
export default ResourceCard